{# First comment #}
{{ string }}
{% if boolA %}
{# Second comment #}
{% for kv in map %}
{{ kv.key }} -- {{ kv.val }}
{% endfor %}
{% elif not (not boolB) %}
Another output
{% else %}
Third branch
{% endif %}
